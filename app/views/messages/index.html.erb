<h1>Your Useful SMS Messages</h1>

<table class='table table-striped table-hover table-responsive'>
  <tr>
    <th>Keyword</th>
    <th>Body</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @messages.each do |message| %>
  <tr>
    <td class="<%= 'danger' if message.keyword.blank? %>"><%= message.keyword %></td>
    <td>
      <%= message.body %>
    </td>
    <td>
      <%= message.description %>
    </td>
    <td>
      <a href="<%= edit_message_path(message) %>" data-toggle="tooltip" title="Edit Message" class="btn btn-default border-blue">
    <span class="glyphicon glyphicon-pencil"></span>
  </a>
    </td>
    <td>
      <%= button_to 'Destroy', message, method: 'delete', confirm: 'Are you sure?', class: "btn btn-danger" %>
    </td>
    <td>
      <%= render "send_sms", message: message%>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Message', new_message_path, remote: true, class: 'btn btn-success' %>
